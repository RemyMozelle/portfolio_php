<section class="row">
  {% for card in cards %}
    <article class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <div class="fix-relative-img">
          <img src="../assets/{{ card.url }}" alt="intégration the montaigne" style="width:100%;height:232px;">
          <figure class="fix-absolute-img">
            <a target="_blank" href="{{ card.github }}">
              <img class="resize-git-view" src="../assets/img/contact/git.png" alt="">
            </a>
          </figure>
        </div>
        <div class="caption">
          <h3>
            {{ card.name }}
          </h3>
          <p>
            {{ card.description }}
          </p>
          <p style="font-size:1em;">
            <?php $techno = explode(',', $cards->technologies); ?>
            <?php $color = explode(',', $cards->color); ?>
            <?php for($i = 0; $i < count($techno); $i++) { ?>
              <span class="label label-default b<?php echo $color[$i]; ?>"><?php echo $techno[$i]; ?></span>
            <?php } ?>
          </p>
        </div>
      </div>
    </article>
  {% endfor %}
</section>